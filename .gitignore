# Rust
/target/

# Recorded runs and viewer pages. These are demo output — regenerate them with
# --dump and analysis/render.py. Real measurement results get added on purpose
# with `git add -f`.
data/*.csv
data/*.html

# Editor settings that make the project build and lint correctly are committed.
# Anything else under .vscode is personal.
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json

# macOS
.DS_Store
